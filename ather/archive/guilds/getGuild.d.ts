import { AtherWebSocket } from "../../types/ather";
import { Guild } from "../../types/ather";
import { Cache } from "../cache";
declare const _default: (id: string, websocket: AtherWebSocket, cache: Cache) => Promise<Guild>;
export default _default;
